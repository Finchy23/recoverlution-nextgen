# Signal / Map Frontend Directive

Build `PLOT` and `∞MAP` as quiet proof rooms inside `ECHO`.

Rules:
- schema-first, not raw-node-first
- proof before spectacle
- spatial field, not dashboard furniture
- focus-zone aware, but not locally authoritative
- degrade gracefully on low-power or reduced-motion devices

Use backend truth from `signal-runtime` only.
Do not invent KBE or cluster doctrine in frontend.

Install now:
- map shell
- proof visualization seam
- focus-zone seam
- event hooks
- fallback / reduced rendering

Do not wire yet:
- local scoring logic
- local cluster semantics
- heavy raw-node first rendering
- speculative intelligence behavior
