# Signal / Map Frontend Directive

Build `PLOT` and `∞MAP` as the proof layer of the Recoverlution organism.

Rules:
- build a living map, not an analytics dashboard
- default to schema-first map behavior
- keep proof honest, calm, and non-gamified
- support `mock shell mode` and `live-contract mode`
- protect backend semantic truth; do not derive it locally

Priorities:
1. `PLOT` room hierarchy
2. schema-first `∞MAP` navigation shell
3. focus-zone interaction law
4. proof and receipt visibility
5. fallback behavior for partial or uncertain signal data
6. event seam for later learning and replay

Install now:
- `signalRuntime` gateway seam
- room adapter/provider seam
- focus-zone interaction seam
- event adapter seam
- fallback/degraded mode seam

Stub now:
- deep KBE interpretation
- high-confidence heat logic
- full node-level landing experience
- long-horizon personal inference

Do not invent:
- telemetry truth
- semantic truth
- focus-zone persistence truth
- proof scoring truth
- clinician/console truth inside the consumer shell

Canonical references:
- `../../integration/backend/40-frontend-runtime-call-index.md`
- `../../integration/backend/54-returnability-measurement-and-learning-framework.md`
- `./SIGNAL_MAP_FRONTEND_AGENT_BRIEF.md`

Success looks like:
- the user can see themselves without feeling managed
- the map is spatial and alive, not analytical sludge
- proof feels real without pretending to know too much
- later runtime hookup does not require a room rewrite
