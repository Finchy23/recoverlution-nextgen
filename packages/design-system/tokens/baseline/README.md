# Token Baseline

This directory stores the approved baseline snapshot used for design token diffing.

Files:

- `canonical.tokens.json`
- `dtcg.tokens.json`
- `token-manifest.json`
- `baseline-metadata.json`

Promote the current build to baseline with:

```bash
npm run tokens:universal:baseline:promote
```
