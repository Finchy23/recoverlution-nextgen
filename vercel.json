{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd \"figma-drop/Command Center Execution Plan\" && npm install",
  "buildCommand": "cd \"figma-drop/Command Center Execution Plan\" && npm run build",
  "outputDirectory": "figma-drop/Command Center Execution Plan/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!.*\\.[^/]+$).*)",
      "destination": "/index.html"
    }
  ]
}
