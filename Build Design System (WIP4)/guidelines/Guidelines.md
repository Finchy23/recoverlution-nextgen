# WIP4 Guidelines

## Purpose
This workspace is the active UI proving ground for the next Figma return.
Design decisions here should strengthen the organism, not flatten it into a conventional app.

## Core rules
- Companion is app-first and intimate.
- The Universal Player remains the Companion shell authority.
- `READ` is the Infinite Book, not a search/library product.
- `ECHO` is the tether/listening layer, not a settings page.
- `PLOT`, `MAP`, and `LINK` should feel complete without carrying backend logic in the UI.

## Do
- preserve atmosphere, hierarchy, and returnability
- keep copy restrained and resonant
- keep shell logic centralized
- use runtime seams instead of direct transport inside surfaces

## Do not
- add generic dashboard gravity to Companion
- add search/results behavior to `READ`
- invent backend truth visually
- let old internal names dictate the user-facing experience
- overexplain what the user is feeling
