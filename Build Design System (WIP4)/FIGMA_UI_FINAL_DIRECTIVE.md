# Figma UI Final Directive

Use `Build Design System (WIP4)` as the live organism reference for the next Figma pass.

## Primary objective
Return with a stronger, cleaner, final UI package for `Companion`.

## Design against these truths
- Companion is app-first and intimate
- the Universal Player remains the shell authority for Companion
- `READ` is the Infinite Book, not a library
- `ECHO` is the tether/listening layer, not settings
- `PLOT`, `MAP`, and `LINK` should feel complete and calm

## Push hardest on
- shell coherence
- room clarity
- `READ` calibration elegance
- `ECHO` polish and biological framing
- copy restraint and resonance

## Do not do
- do not redesign this into a generic wellness app
- do not add content-library behavior to `READ`
- do not add admin gravity to Companion
- do not invent backend truth visually
- do not harden unresolved runtime semantics into doctrine

## Return shape
Bring back:
1. Companion final UI pass
2. `READ` final room pass
3. `ECHO > PLOT / MAP / LINK` final room pass
4. notes where code will need to honor motion, copy, or hierarchy
